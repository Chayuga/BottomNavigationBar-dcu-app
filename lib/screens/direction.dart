import 'package:flutter/material.dart';


class Direction extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Direction Page',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Direction Page'),
        ),
        body: Center(
          child: Text('Direction Page'),
        ),
      ),
    );
  }
}