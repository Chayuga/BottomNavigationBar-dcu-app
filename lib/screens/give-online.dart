import 'package:flutter/material.dart';

class GiveOnline extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Give Online Page',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Give Online Page'),
        ),
        body: Center(
          child: Text('Give Online Page'),
        ),
      ),
    );
  }
}